package ui

// Builder constructs (and sends) the response back to the
// user.  It determines with template pieces to put together,
// assembles them and off they go
type Builder struct {
}

// Assemble gathers the pieces that make up the page, assemble them
// sending the response back to the caller.
func (b *Builder) Assemble() *Page {

}
